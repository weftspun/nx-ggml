defmodule NxGgml do
  @moduledoc """
  A JIT-compiled `Nx` backend that lowers `Nx.Defn` expression graphs into
  ggml compute graphs, executed on CPU or Vulkan GPU compute.

  See `NxGgml.Compiler` for the `Nx.Defn.Compiler` implementation and
  `NxGgml.Backend` for tensor storage.
  """
end
